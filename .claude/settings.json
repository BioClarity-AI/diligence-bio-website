{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -Eq 'git([[:space:]]+-{1,2}[A-Za-z][A-Za-z-]*([[:space:]]+[^[:space:]-][^[:space:]]*)?)*[[:space:]]+push([[:space:]]|$)' && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked by .claude/settings.json. A push to main deploys to bioclarity.ai immediately, so pushes are never automatic here. Commit the work, say what changed, and let the user run the push. See AGENTS.md.\"}}' || true",
            "timeout": 10,
            "statusMessage": "Checking for git push"
          }
        ]
      }
    ]
  }
}
