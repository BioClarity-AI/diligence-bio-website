{
  "name": "diligencebio-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "DiligenceBio marketing site — Astro, statically rendered, deployed to GitHub Pages.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "prepare": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.7.0"
  }
}
